<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.ytazwc.alibaba.mapper.OrderMapper">

    <insert id="createOrder">
        insert into order_tbl (user_id, product_id, count, amount, status, create_time, update_time)
        values (#{userId}, #{productId}, #{count}, #{amount}, #{status}, #{createTime}, #{updateTime})
    </insert>

</mapper>